# URL de la API NestJS (kaivor-api). En local: http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001

# Debe coincidir con el JWT_SECRET del backend para que los tokens sean válidos
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
